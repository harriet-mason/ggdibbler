# default VSUP, can have VSUP variations or stat changes
# e.g. can have it just return mean if you want that variation
# this geom is the same as the sf geom, it just
geom_sf_dist 
  
  



# Default sample, can have ordered or probability variations
# this geom subdivides the area into an grid
# ordered values for prob, 
# geom_sf_sample